import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-private-lable-packing',
  standalone: true,
  imports: [CommonModule, RouterModule],
  templateUrl: './private-lable-packing.html',
  styleUrl: './private-lable-packing.scss',
})
export class PrivateLablePacking {
  // Empty class logic since this page only has standard text + image clone
}
